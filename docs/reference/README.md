# Reference material

Screenshots and external references used to specify future work. Nothing here
is a spec on its own — it's context for whoever picks up the linked backlog item.

## Macro "EdgeFinder-style" scorecard (PARKED — see `docs/FUTURE_WORK.md`)

Drop reference images here, e.g.:

- `edgefinder-forex-scorecard-<PAIR>.png` — the per-pair composite bias gauge +
  6-category sub-scorecard ("X vs. forecast" rows)
- `edgefinder-score-over-time.png` — the score history bar chart
- `edgefinder-economic-heatmap-<COUNTRY>.png` — a per-country heatmap panel

Constraints for that work (unchanged): keep `seed_demo` / `provenance` /
`INSUFFICIENT_EVIDENCE` / read-only behaviour; never fabricate macro data;
contextual intelligence must never become an execution trigger.
