{
  "_comment": "You normally do NOT need this file — run the program and follow the wizard, it writes mt5_agent_config.json for you. This example is only for manual / advanced setups. Never commit the real one.",

  "_normal": "The program already knows the TradeLogger URL. The wizard only asks for these two:",
  "email": "you@example.com",
  "password": "your-tradelogger-login-password",

  "_overrides": "Optional — override the built-in defaults:",
  "server_url": "https://tradelogger-api.onrender.com",
  "poll_minutes": 15,

  "_single_user": "If the site still uses one shared passphrase (no sign-in page), delete email/password above and use this instead:",
  "__passphrase": "the-shared-passphrase",

  "_mt5": "Only if the MT5 terminal is NOT already open and logged in:",
  "mt5": { "login": 0, "password": "", "server": "" }
}
