# Development defaults (committed). Requests stay relative; the Vite proxy
# forwards /api/* to the local FastAPI adapter. No secrets — public bundle.
VITE_API_BASE_URL=
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8000
